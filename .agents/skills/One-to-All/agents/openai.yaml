interface:
  display_name: "One To All"
  short_description: "Repo-specific patterns and workflows for One-to-All"
  default_prompt: "Use the One-to-All repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true